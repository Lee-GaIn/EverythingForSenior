import React from 'react';
import { StyleSheet, View } from 'react-native';

const name = () => {
  return (
    <View>

    </View>
  );
}

export default name;

/*
 + App 
    - Main (nav)
      * Call (nav)
      * Message (nav)
      * Alarm (nav)
      * Photo (nav)
*/